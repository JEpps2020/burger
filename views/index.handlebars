
<div class="row">
  <div class="col-3">

  </div>

 
 <div class="col-3">
 {{#each burgers}}
    {{#unless devoured}}
      {{burger_name}}
        <button class="devoured" data-id="{{id}}" data-newsleep="{{devoured}}">Devour Me!!
        </button>
        <br> 
    {{/unless}}
  {{/each}}
</div>

<div class="col-3">
  {{#each burgers}}
    {{#if devoured}}
    <div id="grayOut"> {{burger_name}}</div>
    {{/if}}
  {{/each}}
</div>
 <div class="col-3">
    
  </div>
</div>

<div class="row text-center">
  <div class="col">
    <form class="create-form">

  <div class="form-group">
    <input type="text" id="ca" name="burger_name">
  <br>

 <button type="submit" class="mainBtn"> Submit</button><br>
 <p>Enter a burger you want to eat Click the "Submit" button.</p>
 </div>

</form>
  </div>
  
</div>


